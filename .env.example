# Divine Relay Manager - Environment Variables
# Copy to .env.local for local development (this file is gitignored)
# Production values are configured in Cloudflare Pages dashboard

# ============================================================================
# Frontend (Vite)
# ============================================================================

# Nostr relay WebSocket URL
# The relay this instance of the admin tool will connect to
VITE_RELAY_URL=wss://relay.divine.video

# ============================================================================
# Worker variables are configured in worker/wrangler.toml or Cloudflare dashboard
# See DEPLOYMENT.md for full configuration guide
# ============================================================================
