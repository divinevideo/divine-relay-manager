# Divine Relay Manager - Environment Variables
# Copy to .env.local for local development (.env.local is gitignored)
# Production values are configured in Cloudflare Pages dashboard

# ============================================================================
# Frontend (Vite) - Environment URLs
# ============================================================================
# Each environment needs both a relay URL (WebSocket) and API URL (HTTP worker)
# Only environments with both URLs configured will appear in the UI

# Production environment
VITE_PROD_RELAY_URL=wss://your-prod-relay.example.com
VITE_PROD_API_URL=https://your-prod-api.example.com

# Staging environment
VITE_STAGING_RELAY_URL=wss://your-staging-relay.example.com
VITE_STAGING_API_URL=https://your-staging-api.example.com

# Legacy environment (optional)
VITE_LEGACY_RELAY_URL=wss://your-legacy-relay.example.com
VITE_LEGACY_API_URL=https://your-legacy-api.example.com

# ============================================================================
# Worker variables are configured in worker/wrangler.toml or Cloudflare dashboard
# See DEPLOYMENT.md for full configuration guide
# ============================================================================
